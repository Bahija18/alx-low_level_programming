#include 'main.h'
#include <stdio.h>
/**
 *@c : char to check
 * _isupper - uppercase characters
 *
 * return : 0 or 1
 */
int _isupper(int c)
{
if (c = 'A' & c = 'a')
return (1);
else
if (c > 'A' & c > 'a')
return (0);
}
